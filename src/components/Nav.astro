---
import { Image } from "astro:assets";
import { business } from "../data/site";
import { brand } from "../data/images";

const navLinks = [
  { href: "#services", label: "Services" },
  { href: "#work", label: "Our Work" },
  { href: "#about", label: "About" },
  { href: "#contact", label: "Contact" },
];
---

<header class="nav">
  <div class="wrap nav__inner">
    <a href="#top" class="nav__brand" aria-label={`${business.name} — home`}>
      <Image
        class="nav__logo"
        src={brand.logoMark.src}
        alt=""
        width={420}
        height={203}
        loading="eager"
      />
      <span class="nav__wordmark">Emergence</span>
    </a>

    <nav class="nav__links" aria-label="Primary">
      {navLinks.map((link) => (
        <a href={link.href}>{link.label}</a>
      ))}
      <a class="nav__phone" href={business.phoneHref}>{business.phone}</a>
    </nav>

    <details class="nav__mobile-details">
      <summary class="nav__toggle">
        <span class="visually-hidden">Menu</span>
        <span class="nav__toggle-bars" aria-hidden="true"></span>
      </summary>

      <div id="mobile-nav" class="nav__mobile">
        <nav aria-label="Primary">
          {navLinks.map((link) => (
            <a href={link.href}>{link.label}</a>
          ))}
        </nav>
        <a class="nav__phone" href={business.phoneHref}>{business.phone}</a>
      </div>
    </details>
  </div>
</header>

<script>
  // Progressive enhancement only: the mobile menu is a native <details>
  // disclosure and works with zero JS. This just closes it after a tap on
  // a link, which native <details> doesn't do on its own.
  const details = document.querySelector<HTMLDetailsElement>(".nav__mobile-details");
  details?.querySelectorAll("a").forEach((link) => {
    link.addEventListener("click", () => {
      details.open = false;
    });
  });
</script>

<style>
  /* Thin, quiet, square. No pill capsule around the links, no filled green
     button — the hero lockup carries the brand, the nav just navigates. */
  .nav {
    position: sticky;
    top: 0;
    z-index: 40;
    background: var(--color-bg);
    border-bottom: var(--rule);
  }

  .nav__inner {
    height: var(--nav-height);
    display: flex;
    align-items: center;
    gap: var(--space-6);
  }

  .nav__brand {
    display: flex;
    align-items: center;
    gap: var(--space-3);
    text-decoration: none;
    color: var(--color-headline);
    margin-right: auto;
    min-width: 0;
  }

  .nav__logo {
    width: auto;
    height: 2.25rem;
  }

  .nav__wordmark {
    font-family: var(--font-display);
    font-variation-settings: "opsz" 48, "SOFT" 0, "WONK" 0;
    font-weight: 500;
    font-size: 1.0625rem;
    letter-spacing: 0.17em;
    text-transform: uppercase;
    white-space: nowrap;
  }

  .nav__links {
    display: none;
    align-items: center;
    gap: var(--space-6);
  }

  .nav__links a {
    font-family: var(--font-body);
    font-size: var(--font-size-sm);
    font-weight: 500;
    letter-spacing: 0.03em;
    text-decoration: none;
    color: var(--color-ink);
    transition: color var(--duration-fast) ease;
  }

  .nav__links a:hover {
    color: var(--color-headline);
  }

  .nav__phone {
    color: var(--color-headline) !important;
    font-weight: 600 !important;
  }

  .nav__mobile-details {
    margin-left: auto;
  }

  .nav__mobile-details > summary {
    list-style: none;
  }

  .nav__mobile-details > summary::-webkit-details-marker {
    display: none;
  }

  .nav__toggle {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    width: 2.5rem;
    height: 2.5rem;
    border: none;
    background: transparent;
    cursor: pointer;
  }

  .nav__toggle-bars,
  .nav__toggle-bars::before,
  .nav__toggle-bars::after {
    display: block;
    width: 1.375rem;
    height: 1px;
    background: var(--color-headline);
    position: relative;
  }

  .nav__toggle-bars::before {
    content: "";
    position: absolute;
    top: -6px;
  }

  .nav__toggle-bars::after {
    content: "";
    position: absolute;
    top: 6px;
  }

  .nav__mobile {
    position: absolute;
    inset-inline: 0;
    top: 100%;
    display: flex;
    flex-direction: column;
    gap: var(--space-4);
    padding: var(--space-5);
    background: var(--color-bg);
    border-bottom: var(--rule);
    opacity: 1;
    transform: translateY(0);
    transition: opacity var(--duration-base) ease, transform var(--duration-base) var(--ease-out);
    transition-behavior: allow-discrete;
  }

  /* entrance-only: native <details> has no JS hook to delay closing, so it
     snaps shut instead of animating out — an acceptable degrade, not a bug */
  @starting-style {
    .nav__mobile-details[open] > .nav__mobile {
      opacity: 0;
      transform: translateY(-8px);
    }
  }

  @media (prefers-reduced-motion: reduce) {
    .nav__mobile {
      transform: none;
    }
  }

  .nav__mobile nav {
    display: flex;
    flex-direction: column;
    gap: var(--space-3);
  }

  .nav__mobile a {
    font-family: var(--font-body);
    font-size: var(--font-size-base);
    text-decoration: none;
    color: var(--color-ink);
  }

  @media (min-width: 64rem) {
    .nav__links {
      display: flex;
    }

    .nav__logo {
      height: 2.5rem;
    }

    .nav__mobile-details {
      display: none;
    }
  }
</style>
